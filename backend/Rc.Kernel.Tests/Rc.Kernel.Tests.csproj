<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- EIN Testvektor fuer beide Seiten. Zwei Kopien waeren genau der Verzug,
         den er verhindern soll: eine Seite wird angefasst, die andere nicht,
         und beide bleiben gruen. -->
    <None Include="../rc-wrap-vector.json" CopyToOutputDirectory="PreserveNewest" Link="rc-wrap-vector.json" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Rc.Kernel\Rc.Kernel.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
