<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    Der Dienst des Neubaus.

    Er liegt DIREKT unter `backend/` und nicht unter `backend/legacy/` — das ist
    die ganze Aussage dieses Umbaus: was hier steht, ist die Plattform; was in
    `legacy/` steht, bedient sie noch, bis sie abgeschaltet wird.

    Er referenziert `Rc.Kernel` und nichts sonst. Der Kernel rechnet mit BYTES
    — Versiegelung, AAD, kanonische Form, Kennungen, Kettenzeilen — und weiss
    von Pfarreien, Gruppen und Ereignissen nichts. Deshalb ueberlebt er die
    Umbenennung, die alles andere trifft, und deshalb waere es ein Fehler, ihn
    neu zu schreiben: geprueft ist er durch 81 Faelle, und eine zweite,
    frisch getippte Kryptografie ist genau die Stelle, an der lautlose Fehler
    entstehen.
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <RootNamespace>Workspace</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.SqlClient" Version="7.0.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Rc.Kernel\Rc.Kernel.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Die Skripte reisen IM Programm mit. Ein Migrationslauf, der seine Dateien
      erst im Dateisystem suchen muss, wendet auf dem Zielrechner etwas anderes
      an als auf dem Entwicklungsrechner.
    -->
    <EmbeddedResource Include="Sql\*.sql" />
  </ItemGroup>

</Project>
